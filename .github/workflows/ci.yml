name: M5 CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: m5-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"

jobs:
  quality:
    name: Lint & Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --group dev

      - name: Ruff lint
        run: uv run ruff check .

      - name: Mypy
        run: uv run mypy

      - name: Summarize
        if: always()
        run: |
          {
            echo "## Lint & Typecheck"
            echo "- Ruff lint: ${{ job.status }}"
            echo "- Mypy on \`src/m5\`: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"

  test:
    name: Pytest (smoke + unit + integration)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run pytest
        run: uv run pytest --cov --cov-report=term-missing

      - name: Summarize
        if: always()
        run: |
          {
            echo "## Tests"
            echo "- pytest: ${{ job.status }}"
            echo "- Commit: \`${{ github.sha }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

  build:
    name: Build wheel & verify install
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Build sdist + wheel
        run: uv build

      - name: Install wheel in a clean venv
        run: |
          uv venv /tmp/verify-venv
          # shellcheck disable=SC1091
          . /tmp/verify-venv/bin/activate
          uv pip install dist/m5-*.whl
          python -c "import m5; print('m5 version:', m5.__version__)"
          m5 --help

      - name: Upload distribution artifacts
        uses: actions/upload-artifact@v4
        with:
          name: m5-dist-${{ github.sha }}
          path: dist/*
          retention-days: 14
          if-no-files-found: error

      - name: Summarize
        if: always()
        run: |
          {
            echo "## Package Build"
            echo "- \`uv build\`: ${{ job.status }}"
            echo "- Wheel install + import + CLI smoke: ${{ job.status }}"
            echo "- Artifacts uploaded as \`m5-dist-${{ github.sha }}\` (14d retention)"
          } >> "$GITHUB_STEP_SUMMARY"
